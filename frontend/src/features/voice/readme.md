# Voice extension point

This directory reserves the frontend boundary for a future WebRTC/SFU integration. It intentionally contains no signaling, media transport, or audio controls. Voice media must never be relayed through the Go match server.
