# Monitoring extension point

Future optional Compose profiles may add an OpenTelemetry collector, Prometheus-compatible metrics store, and dashboards. No monitoring runtime is required by the initial slice.
