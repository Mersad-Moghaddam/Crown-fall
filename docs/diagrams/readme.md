# Diagrams

Mermaid source files are reviewed as architecture documentation and rendered by compatible Markdown tooling. `matchLifecycle.mmd` deliberately uses canonical `EPILOGUE`; see the glossary consistency note.
